# プロジェクト作成

## 0. 仮想環境の追加(Python) 
```sh
uv init <project>
uv add ruff mypy
```

## 2. Claude 初期化
```claude
/init
```

## 3. Git 初期化
```sh
git init .
git branch -M main
```

## 4. ベースコマンド追加

## 5. ベースメモリ追加

## 6. ベースMCP追加
開発で有用なcontext7を追加する

```sh
claude mcp add -s <project> context7 -- npx -y @upstash/context7-mcp
```